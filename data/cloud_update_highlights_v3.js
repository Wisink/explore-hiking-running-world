(async()=>{const db=wx.cloud.database();const _=db.command;console.log('开始更新...');const countRes=await db.collection('routes').count();const total=countRes.total;console.log('云端共',total,'条路线');const routes=[];for(let i=0;i<total;i+=100){const res=await db.collection('routes').skip(i).limit(100).get();routes.push(...res.data)}function genH(r){var n=r.name||'',d=r.description||'',f=r.features||[],s=r.best_season||[],df=r.difficulty||'',c=r.cost||'',t=r.traffic||'',p=[];if(d)p.push(d);if(f.length>0)p.push('沿途可以欣赏到'+f.slice(0,5).join('、')+'等美景。');if(s.length>0)p.push(s.join('和')+'是最佳出行时间。');if(df==='初级'||df==='轻松')p.push('路线轻松平缓，非常适合新手和亲子出行。');else if(df==='中级'||df==='适中')p.push('路线有一定挑战性，适合有一定徒步经验的朋友。');else if(df==='高级'||df==='困难')p.push('路线难度较大，适合经验丰富的户外爱好者挑战。');if(t&&t.length>10)p.push('交通便利，方便到达。');if(cType==='免费')p.push('全程免费，无需门票。');else if(c)p.push('费用参考：'+c+'。');p.push('来'+n+'，感受自然的魅力吧！');return p.join('')}let ok=0,fail=0;for(let i=0;i<routes.length;i+=20){const batch=routes.slice(i,i+20);const tasks=batch.map(r=>{const h=r.highlights||genH(r);return db.collection('routes').doc(r._id).update({data:{highlights:h}}).then(()=>{ok++;console.log('✅',r.name)}).catch(e=>{fail++;console.error('❌',r.name)})});await Promise.all(tasks);console.log('进度:',Math.min(i+20,routes.length)+'/'+routes.length)}console.log('完成！更新',ok,'条，失败',fail,'条')})()