# Bug 修复记录 - 2026-04-22

## 修复的问题

### Bug 1: 世界传送门动画卡死

**问题描述**：
- 从跑步世界-个人中心传送到徒步世界后，再传送到跑步世界
- 进入个人中心时会一直提示"正在传送至徒步世界"
- 反过来操作也会出现同样问题
- 导致个人中心完全无法使用

**根本原因**：
- `showPortal` 设为 `true` 后，跳转前没有重置为 `false`
- 页面切换后，`showPortal` 状态残留，导致下次进入时传送门覆盖层仍然显示

**修复方案**：
- 在 `setTimeout` 回调中，跳转前先调用 `this.setData({ showPortal: false })` 关闭动画
- 修改文件：
  - `miniprogram/pages/running-profile/running-profile.js` - switchToHiking()
  - `miniprogram/pages/profile/profile.js` - switchToRunning()

**修改代码**：
```js
// 修改前
setTimeout(() => {
  wx.setStorageSync('world', 'hiking')
  getApp().globalData.world = 'hiking'
  wx.switchTab({ url: '/pages/routes/routes' })
}, 1200)

// 修改后
setTimeout(() => {
  wx.setStorageSync('world', 'hiking')
  getApp().globalData.world = 'hiking'
  // 先关闭传送门动画，再跳转
  this.setData({ showPortal: false })
  wx.switchTab({ url: '/pages/routes/routes' })
}, 1200)
```

---

### Bug 2: 跑步世界搜索页面布局混乱

**问题描述**：
- 跑步世界点击搜索框进入搜索界面
- 搜索界面的 UI 布局是乱的

**根本原因**：
1. **CSS 变量未定义**：`results-scroll` 使用 `var(--status-bar-height, 20px)` 但 CSS 变量从未定义
2. **WXML/JS 不匹配**：
   - WXML 绑定 `onKeywordInput`，JS 定义 `onInput`
   - WXML 使用 `searched`，JS 定义 `searchDone`
   - WXML 使用 `hotKeywords`、`onHotTagTap`、`onArticleTap`、`onBack`，JS 中缺失或命名不同

**修复方案**：

1. **修复 CSS**：将 CSS 变量改为固定计算值
```css
/* 修改前 */
.results-scroll {
  height: calc(100vh - var(--status-bar-height, 20px) - 44px - 112rpx);
}

/* 修改后 */
.results-scroll {
  /* 状态栏约20-44px + 导航栏44px + 搜索栏约56px = 约120-144px */
  height: calc(100vh - 150px);
}
```

2. **修复 JS/WXML 不匹配**：
   - 添加 `onKeywordInput` 方法（别名调用 `onInput`）
   - 将 `searchDone` 改为 `searched`
   - 添加 `hotKeywords` 数据
   - 添加 `onHotTagTap` 方法
   - 添加 `onArticleTap` 方法（别名调用 `onResultTap`）
   - 添加 `onBack` 方法（别名调用 `onBackTap`）
   - 添加 `onScrollToLower` 方法

**修改文件**：
- `miniprogram/pages/running-search/running-search.js` - 重写，匹配 WXML
- `miniprogram/pages/running-search/running-search.wxss` - 修复高度计算

---

### Bug 3: 搜索页面UI风格不统一

**问题描述**：
- 搜索页面是白色底，不好看
- 顶部标题栏的"文章搜索"几个字没有居中
- 标题栏随页面滚动而滚动

**修复方案**：

完全重写搜索页面，使用跑步世界的蓝色渐变+毛玻璃风格：

1. **背景**：蓝色渐变 + 多层径向渐变装饰
2. **标题栏**：固定定位 + 毛玻璃效果 + 标题居中
3. **搜索栏**：毛玻璃卡片效果
4. **搜索历史**：毛玻璃标签
5. **热门搜索**：半透明标签
6. **搜索结果卡片**：毛玻璃卡片效果

**修改文件**：
- `miniprogram/pages/running-admin/article-edit.wxml` - 重写，统一数据绑定
- `miniprogram/pages/running-admin/article-edit.wxss` - 重写，增加输入框高度
- `miniprogram/pages/running-admin/article-edit.js` - 重写，修复方法缺失和数据问题

---

## 测试建议

1. **测试 Bug 1**：
   - 进入跑步世界 → 个人中心 → 点击"传送至徒步世界"
   - 等待传送动画完成，进入徒步世界首页
   - 点击个人中心 → 点击"传送至跑步世界"
   - 等待传送完成，进入跑步世界首页
   - 点击个人中心 → 确认没有传送门覆盖层

2. **测试 Bug 2 & 3**：
   - 进入跑步世界 → 点击搜索框
   - 确认搜索页面布局正常（蓝色渐变背景、毛玻璃效果）
   - 确认标题栏固定在顶部，标题居中
   - 点击热门搜索标签，确认能正常搜索
   - 输入关键词搜索，确认结果列表正常显示

3. **测试 Bug 4**：
   - 进入跑步世界 → 后台管理 → 新增文章
   - 确认标题栏高度超过胶囊按钮底部
   - 确认标题、阅读时长、排序权重、标签的输入框高度正常
   - 点击"难度等级"下拉，确认有4个选项
   - 选择频道和子分类，确认选择后正确显示
   - 点击返回按钮，确认能正常返回

---

## 语法检查

所有修改文件已通过语法检查：
- ✅ miniprogram/pages/running-profile/running-profile.js
- ✅ miniprogram/pages/profile/profile.js
- ✅ miniprogram/pages/running-search/running-search.js
- ✅ miniprogram/pages/running-search/running-search.wxml
- ✅ miniprogram/pages/running-search/running-search.wxss
- ✅ miniprogram/pages/running-admin/article-edit.js
- ✅ miniprogram/pages/running-admin/article-edit.wxml
- ✅ miniprogram/pages/running-admin/article-edit.wxss
