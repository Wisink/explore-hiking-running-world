/* pages/knowledge/knowledge.wxss - v3.0 自然清新风 + 毛玻璃 */

/* ===== CSS Variables ===== */
:host {
  --glass-bg: rgba(255, 255, 255, 0.22);
  --glass-bg-light: rgba(255, 255, 255, 0.12);
  --glass-bg-strong: rgba(255, 255, 255, 0.35);
  --glass-border: rgba(255, 255, 255, 0.25);
  --glass-blur: blur(16px);
  --glass-shadow: 0 8rpx 32rpx rgba(31, 38, 50, 0.18);
  --text: #1a2e1a;
  --text2: #4a6b4a;
  --primary: #4CAF50;
  --primary-dark: #2d6a4f;
  --r: 36rpx;
  --tr: all 350ms cubic-bezier(0.4, 0, 0.2, 1);
}

.knowledge-page {
  min-height: 100vh;
  padding-bottom: 140rpx;
  background:
    radial-gradient(ellipse at 20% 50%, rgba(129,199,132,.6) 0%, transparent 50%),
    radial-gradient(ellipse at 80% 20%, rgba(165,214,167,.5) 0%, transparent 50%),
    radial-gradient(ellipse at 60% 80%, rgba(76,175,80,.4) 0%, transparent 50%),
    radial-gradient(ellipse at 10% 90%, rgba(200,230,201,.6) 0%, transparent 40%),
    linear-gradient(160deg, #e8f5e9 0%, #c8e6c9 25%, #a5d6a7 50%, #81c784 75%, #66bb6a 100%);
}

/* 顶部标题 — 毛玻璃 */
.page-header {
  position: relative;
  padding: 48rpx 32rpx 40rpx;
  margin-bottom: 16rpx;
  overflow: hidden;
}

.page-header__bg {
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: linear-gradient(135deg, rgba(129,199,132,.35) 0%, rgba(200,230,201,.25) 100%);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-bottom: 1px solid rgba(255,255,255,.18);
}

.page-header__content {
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.page-header__emoji { font-size: 48rpx; margin-bottom: 8rpx; }

.page-header__title {
  font-size: 44rpx;
  font-weight: 800;
  color: #1a2e1a;
  letter-spacing: 1rpx;
  text-shadow: 0 1rpx 6rpx rgba(255,255,255,.5);
}

.page-header__sub {
  font-size: 26rpx;
  color: #6b8b6b;
  letter-spacing: 2rpx;
}

/* 通用区块 */
.section { margin: 0 24rpx 36rpx; }

.section__head {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
  padding: 0 8rpx;
}

.section__title-wrap {
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.section__icon { font-size: 32rpx; }

.section__title {
  font-size: 34rpx;
  font-weight: 700;
  color: #1a2e1a;
}

.section__count {
  background: rgba(129, 199, 132, 0.2);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  padding: 6rpx 16rpx;
  border-radius: 20rpx;
  font-size: 22rpx;
  color: #2d6a4f;
  font-weight: 500;
  border: 1px solid rgba(129,199,132,.15);
}

/* 推荐阅读横向滚动 — 毛玻璃 */
.recommend-scroll {
  white-space: nowrap;
  overflow: hidden;
}

.recommend-card {
  display: inline-flex;
  flex-direction: column;
  width: 420rpx;
  background: rgba(255, 255, 255, 0.22);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255,255,255,.25);
  border-radius: 20rpx;
  margin-right: 20rpx;
  overflow: hidden;
  box-shadow: 0 8rpx 32rpx rgba(31,38,50,.15);
  transition: transform 200ms ease;
}

.recommend-card:active { transform: scale(0.97); }

.recommend-card__cover {
  height: 180rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, rgba(129,199,132,.5), rgba(76,175,80,.4));
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
}

.recommend-card__icon { font-size: 64rpx; opacity: 0.9; }

.recommend-card__info {
  padding: 20rpx 24rpx 24rpx;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.recommend-card__tag {
  align-self: flex-start;
  padding: 4rpx 14rpx;
  border-radius: 12rpx;
  background: rgba(129, 199, 132, 0.15);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  font-size: 20rpx;
  color: #2d6a4f;
  font-weight: 500;
  border: 1px solid rgba(129,199,132,.15);
}

.recommend-card__title {
  font-size: 30rpx;
  font-weight: 700;
  color: #1a2e1a;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.recommend-card__summary {
  font-size: 24rpx;
  color: #6b8b6b;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 文章列表 — 毛玻璃 */
.article-list {
  background: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  border: 1px solid rgba(255,255,255,.2);
  border-radius: 20rpx;
  box-shadow: 0 4rpx 16rpx rgba(31,38,50,.1);
  overflow: hidden;
}

.article-item {
  display: flex;
  align-items: center;
  padding: 28rpx 24rpx;
  border-bottom: 1px solid rgba(255,255,255,.12);
  transition: background 150ms ease;
}

.article-item:last-child { border-bottom: none; }

.article-item--hover {
  background: rgba(255,255,255,.1);
}

.article-item__icon {
  width: 80rpx;
  height: 80rpx;
  border-radius: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 20rpx;
  flex-shrink: 0;
  font-size: 36rpx;
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  border: 1px solid rgba(255,255,255,.2);
}

.article-item__icon--equip {
  background: rgba(200, 230, 201, 0.5);
}

.article-item__icon--safety {
  background: rgba(255, 205, 210, 0.5);
}

.article-item__icon--manner {
  background: rgba(225, 190, 231, 0.5);
}

.article-item__body {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.article-item__title {
  font-size: 30rpx;
  font-weight: 600;
  color: #1a2e1a;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.article-item__summary {
  font-size: 24rpx;
  color: #6b8b6b;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.article-item__arrow {
  font-size: 36rpx;
  color: #6b8b6b;
  font-weight: 300;
  margin-left: 12rpx;
  flex-shrink: 0;
}

/* 加载状态 — 毛玻璃 */
.loading-card {
  background: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255,255,255,.2);
  border-radius: 24rpx;
  padding: 80rpx 60rpx;
  margin: 40rpx 24rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 24rpx;
  box-shadow: 0 4rpx 24rpx rgba(31,38,50,.12);
}

.loading-spinner {
  width: 48rpx;
  height: 48rpx;
  border: 4rpx solid rgba(255,255,255,.3);
  border-top-color: #2d6a4f;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin { to { transform: rotate(360deg); } }

.loading-text { font-size: 26rpx; color: #6b8b6b; }

/* 底部 */
.footer {
  text-align: center;
  padding: 48rpx 24rpx 32rpx;
}

.footer__icon { font-size: 48rpx; margin-bottom: 12rpx; display: block; }

.footer__main {
  font-size: 28rpx;
  color: #4a6b4a;
  font-weight: 600;
  display: block;
  margin-bottom: 8rpx;
}

.footer__sub { font-size: 22rpx; color: #6b8b6b; }

/* 分类快速入口 — 毛玻璃 */
.category-cards {
  display: flex;
  gap: 16rpx;
  padding: 24rpx;
}

.category-card {
  flex: 1;
  background: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  border: 1px solid rgba(255,255,255,.2);
  border-radius: 16rpx;
  padding: 24rpx 16rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  box-shadow: 0 2rpx 8rpx rgba(31,38,50,.08);
}

.category-card--on {
  background: rgba(200, 230, 201, 0.4);
  border-color: rgba(129,199,132,.3);
  box-shadow: 0 2rpx 12rpx rgba(76,175,80,.15);
}

.category-card__icon { font-size: 48rpx; }
.category-card__title { font-size: 26rpx; font-weight: 600; color: #1a2e1a; }
.category-card__count { font-size: 22rpx; color: #6b8b6b; }

/* 查看更多按钮 */
.show-more {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 20rpx 0 10rpx;
}

.show-more__text {
  font-size: 26rpx;
  color: #2d6a4f;
  font-weight: 500;
}
